"""Backward compatibility — imports from core.hooks."""
from de_funk.core.hooks import (
    pipeline_hook,
    discover_plugins,
    BuildPluginRegistry,
    HookRunner,
    _decorator_registry,
    _get_decorator_hooks,
)
